## 2026-06-15 - Removed redundant regex compilation in gateway/run.py
**Learning:** Avoid redefining variables like `_TOOL_MEDIA_RE` inside tight loops when it's already defined at the module-level. While small, moving the regex outside the loop prevents redundant object creation and slightly improves loop iteration efficiency. The module-level definition `_TOOL_MEDIA_RE` at line 683 in `gateway/run.py` is fully functional and covers the same exact regex, so the inner loop compilation at line 17742 was removed without any issue.
**Action:** Always search the module or class scope to see if a variable is already created before defining a redundant one inside a loop structure. Use `pytest` on targeted testing scopes immediately after refactoring code paths in `gateway/run.py` to ensure module variables reference properly.
